main () {
    int i,j,k, l, m=-1 , product;
    int c;
    outputint (m);
    i = inputint;
    j = inputint;
    c = inputchar+1;
    k = i+j;
    l = product = i*j;
    outputint (k);
    outputint (l);
    outputint (product);
    outputchar (c);
}
