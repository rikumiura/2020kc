main() {
    int number[] = {-1, 0,1};
    int message[] = {'a', 'r', 'r', 'a', 'y'};
    int i=0;
    while (i< 3) {
        outputint(number[i++]);
    }

    i=0;
    while (i< 5) {
        outputchar(message[i++]);
    }
}
